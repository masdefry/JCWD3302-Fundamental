const products = [
  {
    name: 'Anggur',
    price: 10000,
  },
  {
    name: 'Apel',
    price: 15000,
  },
];

products[0]; // Constant time

// Indexing/binary search -> binary three

const arr = [['a', 1], 'b', 'c', 'd'];

for (let item of arr) {
  // 3
  for (let item1 of arr) {
    // 1x3 -> 1x3 -> 1x3
  }
};